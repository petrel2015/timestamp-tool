# 更新日志

本项目所有对用户可见的重要变更记录于此。格式遵循 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)。

> **版本说明。** 本仓库没有 git tag、没有 GitHub Release、也没有 `package.json`（2026-08-27 核查：`git tag` 为空、`gh release list` 为空）。因此下方的 `1.0.0` 是针对已发布站点的**声明的汇总版本**——把创始提交（两笔均为 2026-08-27）时的完整能力集聚合为一条。后续变更先记入 `[Unreleased]`，待维护者打下首个正式 tag 后再拆分，并删除本说明。

## [Unreleased]

### 新增

- 完整双语文档体系：重构 `README.md` / `README.zh.md`，新增 `README_FOR_AI.md`、本更新日志（中英）、`docs/en` + `docs/zh` 全套页面（使用 / 开发 / 部署 / 故障排查 / 隐私 / 常见问题）及 `docs/*/features/` 下的赞赏功能设计文档；截图均为真实浏览器截取，存于 `docs/img/`。（原 `README.zh-CN.md` 更名为 `README.zh.md`。）

### 变更

- **转换区提为页面首位**：双向转换成为 01 分区、置于实时时钟（02）之上——任何屏幕尺寸下用户首先看到的都是输入框，手机上无需再划过整块时钟。
- 输入区视觉强调：输入框加大（19 px 字号、56 px 高度），两张卡片标题前置红色方块标记，聚焦所在卡片出现红色顶边、输入框内红色光标与下划线，结果面板背景压灰，使白色输入卡成为视觉上层。
- 移动端打磨：单位选择器与「采用当前时间」按钮全宽，按压（`:active`）反馈并去除系统点击高亮，iOS 日期 / 时间控件归一（字号 ≥ 16 px，聚焦不触发缩放），堆叠输入卡之间补发丝分隔线，页脚适配安全区（`env(safe-area-inset-bottom)`），并为移动端浏览器导航条新增 `theme-color` meta。

## [1.0.0] - 2026-08-27

首次发布；本条目汇总初始站点的完整功能集（两笔创始提交均为 2026-08-27）。

### 新增

- 瑞士风格、中英双语的一页式 Unix 时间戳转换工具：实时时钟（星期、时区标签、可复制的当前 Unix 秒 / 毫秒）。
- 双向转换——时间戳 → 日期时间（支持负数、容忍空格 / 逗号 / 下划线，单位自动识别：不超过 11 位按秒、12–15 位按毫秒、16 位及以上按微秒；也可强制单位）；日期时间 → 时间戳（原生选择器 + 「采用当前时间」快捷键）。
- 统一结果面板：本地时间、UTC 时间、ISO 8601、Unix 秒、Unix 毫秒、相对时间——全部可复制。
- 「当前时间」与转换结果两套纪元统计：自 1970-01-01 起的天 / 周 / 月计数、年内第几天、ISO 8601 周号（口径见使用文档）。
- URL 参数预填：`?ts=`。
- 语言检测（浏览器语言）、顶栏切换、localStorage 持久化（`ts-lang`）。
- 响应式布局（手机单列、桌面双列网格）；手写 CSS，无框架、无构建步骤；支持从磁盘直接打开。
- 赞赏功能：页脚入口、弹窗（焦点圈定 / ESC / 遮罩关闭）、运行时生成的支付宝 / 微信支付二维码（vendor 锁定 `qrcode-generator` v1.4.4，首次打开时懒加载）、手机端支付宝跳转软检测与二维码兜底。
- 赞赏功能 E2E 测试（Playwright + jsQR 解码，桌面 + 移动流程，31 项断言）。
